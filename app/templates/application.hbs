<div class="container" id="main">
    <div class="row">
        <div class="col-md-12">
            {{#each}}
                {{income-table data=data totalIncome=sum}}
            {{/each}}
        </div>
    </div>
</div>
